// @flour-city/email-core — single entry point.
// Re-exports the pure parser + analyzer so consumers can import everything from
// one place: `import { parseReceivedChain, analyze } from '@flour-city/email-core'`.
export * from './parser.js';
export * from './analyzer.js';
